# Core UI
streamlit>=1.45.0

# Anthropic SDK (already installed, pinned for reproducibility)
anthropic>=0.97.0

# MCP (Model Context Protocol) SDK
mcp[cli]>=1.9.0

# FastAPI + Uvicorn for MCP servers and load test API endpoint
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# Monitoring
prometheus_client>=0.25.0

# OpenTelemetry — traces, metrics, OTLP export (Step 15)
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp-proto-grpc>=1.25.0
opentelemetry-semantic-conventions>=0.46b0

# Graph database client (Step 18)
neo4j>=5.20.0

# Load testing
locust>=2.34.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-mock>=3.14.0

# Env vars
python-dotenv>=1.0.0

# Structured logging (already installed)
python-json-logger>=3.0.0

# Data validation (already installed)
pydantic>=2.13.0
