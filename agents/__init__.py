"""
Multi-agent system:
  CustomerAgent    — customer context & segmentation
  KnowledgeAgent   — RAG-backed policy retrieval
  EligibilityCheckerAgent — tool-use eligibility loop
  RiskAssessorAgent       — composite risk band
  ExplainerAgent          — rule-based verdict + LLM explanation
  ComplianceAgent         — regulatory compliance validation
  AuditAgent              — structured audit record generation
  OrchestratorAgent       — autonomous planning + delegation hub
"""
